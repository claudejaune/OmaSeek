/**
 * \`omaseek-pixel\` has nothing to do on the Node side: the field reads the page
 * it is already on. A browser half still has to arrive as a plugin row, and a
 * row is a plugin, so this is the empty one.
 */

export function apply() {}
