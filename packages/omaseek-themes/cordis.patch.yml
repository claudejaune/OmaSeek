# The OmaThemes row on its own: install this package directly to run the themes
# and the picker without the other two. OmaPixel and OmaMusic are separate
# packages with rows of their own — install whichever you want, in any
# combination.
- insert:
    - id: omaseek-themes
      name: omaseek-themes
